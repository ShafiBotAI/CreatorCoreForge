# Auto-generated for Support visual memory handoff to other creator accounts or teams
from typing import Dict


def support_visual_memory(memory: Dict[str, str], export: bool = False) -> Dict[str, str]:
    """Return a copy of ``memory`` suitable for handoff."""

    return dict(memory) if export else memory
