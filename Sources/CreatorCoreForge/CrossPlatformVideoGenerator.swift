import Foundation

/// Generates videos in a format appropriate for the current platform.
public final class CrossPlatformVideoGenerator {
    public init() {}

    /// Echoes back the frames for now.
    public func generate(frames: [String]) -> [String] {
        frames
    }
}
