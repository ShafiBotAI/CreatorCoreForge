import Foundation

/// Stub AR/VR playback helper.
public struct ARVRPlayback {
    public init() {}
    /// Returns a message for playing a scene in AR/VR mode.
    public func play(scene: String) -> String {
        "Playing \(scene) in AR/VR"
    }
}
